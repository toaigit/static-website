resource "aws_route53_record" "web" {
  zone_id  = var.route53_zone_id
  name     = var.staticsite
  type     = "A"

  alias {
    name   = aws_cloudfront_distribution.s3_distribution.arn
    zone_id = var.cf_hosted_zone_id
    evaluate_target_health = false
  }
}
